import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'co.edu.ue.qrbarras',
  appName: 'lector-qr-barras',
  webDir: 'dist/lector-qr-barras/browser'
};

export default config;
